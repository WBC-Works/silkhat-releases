# SilkHat Releases

Auto-update feed for the SilkHat desktop app: signed installers + `latest.json`.

Source is private (`WBC-Works/silkhat`); these artifacts are minisign-signed. The `stable` release is the rolling auto-update channel.
